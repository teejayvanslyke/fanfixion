<div id="trainer">

  <h5>Emo doesn't understand emotions like we do, so we have to teach it.  You can help train emo by reading a few tweets and answering some simple questions.</h5>

  <div class="status"><%= @status.text %></div>

  <h5>Does @<%= @status.twitter_user_screen_name %> feel <span class="emotion"><%= @emotion.name %></span> because of <span class="trend"><%=h @status.trend.name %></span>?</h5>

  <div class="span-8">
    <% form_tag sentiments_path do -%>
      <%= hidden_field_tag :emotion_id, @emotion.id %>
      <%= hidden_field_tag :status_id, @status.id %>
      <%= hidden_field_tag :match, true %>
      <%= submit_tag 'Yes' %>
    <% end -%>
  </div>

  <div class="span-8">
    <% form_tag sentiments_path do -%>
      <%= hidden_field_tag :emotion_id, @emotion.id %>
      <%= hidden_field_tag :status_id, @status.id %>
      <%= hidden_field_tag :match, false %>
      <%= submit_tag 'No' %>
    <% end -%>
  </div>

  <div class="span-8 last">
    <%= button_to "Irrelevant / Can't Tell / Spam", trainer_path %>
  </div>
</div>
